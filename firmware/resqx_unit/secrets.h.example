// Copy this file to secrets.h, then fill in the robot's network settings.
// secrets.h is ignored by Git so the Wi-Fi password is never committed.
#pragma once

const char* WIFI_SSID = "YOUR_WIFI_SSID";
const char* WIFI_PASSWORD = "YOUR_WIFI_PASSWORD";
const char* MQTT_HOST = "20.32.3.147";
const uint16_t MQTT_PORT = 1883;
const char* UNIT_ID = "unit-01";
