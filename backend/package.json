{
  "name": "resqx-backend",
  "version": "1.0.0",
  "description": "RESQ-X control server: ingests sensor/AI data from the rescue unit over MQTT, scores risk, pushes live updates to the dashboard, and relays movement/control commands back to the hardware.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "simulator": "node src/simulator.js",
    "init-admin": "node scripts/init-admin.cjs",
    "reset-password": "node scripts/reset-password.cjs"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mqtt": "^5.7.0",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
